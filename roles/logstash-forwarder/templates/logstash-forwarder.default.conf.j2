###########################################
# Default settings for logstash-forwarder #
###########################################

# Set a home directory
#LS_HOME=/var/lib/logstash-forwarder

# Arguments to pass to logstash agent
#LS_OPTS=""

# pidfiles aren't used for upstart; this is for sysv users.
#LS_PIDFILE=/var/run/logstash-forwarder.pid

# user id to be invoked as; for upstart: edit /etc/init/logstash.conf
#LS_USER=logstash

# logstash logging
#LS_LOG_FILE=/var/log/logstash-forwarder/logstash-forwarder.log
#LS_USE_GC_LOGGING="true"

# logstash configuration directory
#LS_CONF_DIR=/etc/logstash-forwarder

# Open file limit; cannot be overridden in upstart
#LS_OPEN_FILES=16384

# Nice level
#LS_NICE=19
